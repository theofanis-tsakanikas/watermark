"""The synthetic operator: a fixed cast, a seeded event stream, and every pathology on purpose.

`docs/SCENARIO.md` ends with the sentence this package exists to satisfy: *a synthetic dataset
with no pathology proves nothing.* Everything difficult about this domain is generated here on
purpose, each thing labelled, so that a harness can assert an exact count rather than "no
exception was raised".
"""
